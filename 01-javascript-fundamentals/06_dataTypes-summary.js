// Primitive Data Types (7 total)

let a = 5;              // Number   — max: Number.MAX_VALUE (~1.7976931348623157e+308)
let b = "Motherboard";  // String   — text, in quotes
let c = true;           // Boolean  — true / false
let d = null;           // Null     — intentionally empty (not the same as 0)
let e = undefined;      // Undefined — declared but no value assigned
let f = Symbol("id");   // Symbol   — guaranteed unique value, often used as object keys
let g = 10n;            // BigInt   — integers with no upper limit, no precision loss

console.log(f);


// Non-Primitive Data Types (Array, Objects, Functions)
